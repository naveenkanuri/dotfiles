# Neovim Config

AstroNvim v6 config focused on Go/Rust, debugging, database work, GitLab review, and CodeCompanion repo sessions.

```shell
git clone <repo> ~/.config/nvim
nvim
```

`lazy-lock.json` is tracked; use `:Lazy sync` for reproducible plugin installs.
